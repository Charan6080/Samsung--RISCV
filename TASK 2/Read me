The output of both -O1 and -Ofast format will be equal when the c code is simple and direct.
Because -Ofast runtime is faster due to aggressive optimizations ,so it commonly produces shorter output lengths.
-O1 format enables basic optimizations while keeping the compilation time short.
